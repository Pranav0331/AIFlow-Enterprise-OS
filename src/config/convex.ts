/**
 * Paste the Convex deployment URL here after running the Convex CLI.
 *
 * Steps (run locally, outside Enter):
 *   1. npx convex login
 *   2. npx convex dev        -> prints a deployment URL like
 *                               https://happy-animal-123.convex.cloud
 *   3. Paste that URL below.
 *
 * Enter does not support VITE_* environment variables, so the URL is kept
 * here as a plain constant instead of an env var.
 */
export const CONVEX_URL = "https://outstanding-peacock-486.eu-west-1.convex.cloud";
